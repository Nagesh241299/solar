import { environment } from '../environments/environment';

export const envDetails = {
  apiUrl: environment.apiUrl,
  boqBuilderUrl: environment.boqBuilderUrl,
  googlemapKey: environment.googlemapKey
};